package tests.races;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Assertions;

import races.Humain;
import races.Elfe;
import races.Nain;
import races.Halfelin;
/*
public class RaceTest {

    @Test
    public void testCreationHumain() {
        Humain humain = new Humain();
        Assertions.assertNotNull(humain);
        Assertions.assertTrue(humain.getNom().toLowerCase().contains("humain"), "Le nom de l'humain doit contenir 'humain'");
    }

    @Test
    public void testCreationElfe() {
        Elfe elfe = new Elfe();
        Assertions.assertNotNull(elfe);
        Assertions.assertTrue(elfe.getNom().toLowerCase().contains("elfe"));
    }

    @Test
    public void testCreationNain() {
        Nain nain = new Nain();
        Assertions.assertNotNull(nain);
        Assertions.assertTrue(nain.getNom().toLowerCase().contains("nain"));
    }

    @Test
    public void testCreationHalfelin() {
        Halfelin halfelin = new Halfelin();
        Assertions.assertNotNull(halfelin);
        Assertions.assertTrue(halfelin.getNom().toLowerCase().contains("halfelin"));
    }
}
*/